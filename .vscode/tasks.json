{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Publish Docs and Deploy",
      "type": "shell",
      "command": "set -e && python -m mkdocs build && git add . && (git diff --cached --quiet || git commit -m \"Update docs\") && git push && python -m mkdocs gh-deploy --clean",
      "options": {
        "cwd": "${workspaceFolder}",
        "shell": {
          "executable": "C:\\Program Files\\Git\\bin\\bash.exe",
          "args": ["-c"]
        }
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
